<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iss">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/background_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".delete"
            android:label="Delete Products"
            android:theme="@style/Theme.AppCompat.Dialog.Alert" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".DetaliiProdus_Admin" />
        <activity android:name=".DisplayCatalog_PtAdmin" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LoginActivity"></meta-data>
        </activity>
        <activity
            android:name=".CumparaProdus"
            android:label="Your Order"
            android:theme="@style/Theme.AppCompat.Dialog.Alert" />
        <activity android:name=".DetaliiProdus" />
        <activity android:name=".AdaugaProdus" />
        <activity android:name=".DisplayCatalog" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LoginActivity"></meta-data>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>